# Python - Exceptions

This project introduces **errors and exceptions in Python**, how they work, why they occur, and how to properly handle them using `try`, `except`, `else`, `finally`, and `raise`.
